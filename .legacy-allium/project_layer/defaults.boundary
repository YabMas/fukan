-- boundary: 1

-- fukan-on-fukan default project registry.

use "./registry.allium" as registry

fn fukan_on_fukan() -> registry.Registry
  -- Project registry for fukan analyzing itself. Returns the
  -- identity registry.
