-- boundary: 1

-- Phase 4d module-visibility rules — pure check entry point. See
-- rules_4d.allium for the module-visibility invariants.

use "../model/spec.allium" as model
use "../agent/api.allium" as agent

fn check(model: model.Model) -> List<agent.Violation>
    -- Run all 4d module-visibility rules against the Model and
    -- return the aggregated Violation sequence.
