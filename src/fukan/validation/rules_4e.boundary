-- boundary: 1

-- Phase 4e subsystem-visibility rules — pure check entry point. See
-- rules_4e.allium for the subsystem-visibility invariants.

use "../model/spec.allium" as model
use "../agent/api.allium" as agent

fn check(model: model.Model) -> List<agent.Violation>
    -- Run all 4e subsystem-visibility rules against the Model and
    -- return the aggregated Violation sequence.
