-- boundary: 1

-- Phase 4c binding rules — pure check entry point. See
-- rules_4c.allium for the five binding invariants.

use "../model/spec.allium" as model
use "../agent/api.allium" as agent

fn check(model: model.Model) -> List<agent.Violation>
    -- Run all five 4c binding rules against the Model and return
    -- the aggregated Violation sequence.
