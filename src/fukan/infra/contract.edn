{:description "Lifecycle management via Integrant components. The model component (:fukan.infra/model) returns an atom that the handler derefs per-request. The server component (:fukan.infra/server) starts/stops http-kit. Each is independently restartable."
 :functions [fukan.infra.model/load-model!
             fukan.infra.model/refresh!]}
