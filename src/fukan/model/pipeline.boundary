-- boundary: 1

-- Model construction.

use "./spec.allium" as model

fn build_model(src: FilePath) -> Model
  -- Build complete model from a source path.

exports:
    AnalysisResult
    LintViolation
    LintReport
    LintStats
